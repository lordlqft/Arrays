/* Crie um array para 5 números inteiros (digitados pelo usuário) e calcule a soma de todos os
valores. */


/*
int[] numeros = new int[6];

for (int i = 0; i < 6; i++)
{
    Console.Write($"Digite o {i + 1}º número: ");
    numeros[i] = int.Parse(Console.ReadLine()!);
}

Console.WriteLine();
Console.WriteLine("Soma de todos os números do array:");
int soma = 0;

for (int i = 0; i < 6; i++)
{
    soma += numeros[i];
}

Console.WriteLine(soma);
*/